extends base

block content
  main.main
    .error
      .error__title
        h2.heading-secondary.heading-secondary--error Uh oh! Something went wrong!   
        h2.error__emoji 😢 🤯
      a.btn.btn--green.span-all-rows(href=`intent:#Intent;scheme=qssneaker://users/resetPassword/${token};package=com.yenmaiteam.qssneaker;end`) Reset your password!
